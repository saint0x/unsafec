#[repr(C)]
struct HashLane {
    a: i32,
    b: i32,
    c: i32,
    d: i32,
}

#[repr(C)]
struct BenchConfig {
    seed: i32,
    rounds: i32,
    workers: i32,
}

#[repr(C)]
enum BenchMode {
    COnly,
    FzyOnly,
    Dual,
}

fn schema_version() -> i32 {
    return 2;
}
